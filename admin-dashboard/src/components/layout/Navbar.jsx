const Navbar = () => {
    return(
        <div style={styles.nav}>
            <h3>Admin Dashboard</h3>
        </div>
    );
};

const styles = {
    nav: {
        background: "#fff",
        padding: "10px",
        borderbottom: "1px solid #ddd"
    }
};

export default Navbar;